/bin/
/dist/
*.test
*.out
coverage.*
.env
.env.*
!.env.example
.DS_Store
node_modules/

# Local working notes — not published
docs/
.gstack/

# The dashboard's build inputs; embed/dist is committed so a Go build never
# needs node.
web/node_modules/
web/dist/
